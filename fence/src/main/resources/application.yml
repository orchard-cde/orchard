server:
  port: 7779

spring:
  application:
    name: fence

fence:
  signing-key:
    path: ${FENCE_SIGNING_KEY_PATH:${user.home}/.fence/signing-key.jwk}

management:
  endpoints:
    web:
      exposure:
        include: health,info
